package aog.app.vectorseq;

import aog.app.sequence.SequenceCombiner;

public class VectorSeqCombiner extends SequenceCombiner {
	/*
	 * Currently it is exactly the same as SequenceCombiner
	 */
}
